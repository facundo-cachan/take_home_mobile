const marginHorizontal: number = 15
const tapbar = `flex flex-row w-50`
const styles = {
  left: `${tapbar} ml-[-${marginHorizontal}]`,
  right: `${tapbar} mr-[-${marginHorizontal}] pr-5`,
}

export default styles
